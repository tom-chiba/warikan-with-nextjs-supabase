# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#

pre-commit:
  jobs:
    - group:
        parallel: true
        jobs:
          - run: npm run tsc
          - run: npm run biome:check
    - run: npm run build
